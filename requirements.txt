# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.35
asyncpg>=0.29.0
alembic>=1.13.2

# ── Validation & configuration ─────────────────────────────────────────────────
pydantic>=2.9.1
pydantic-settings>=2.5.2

# ── HTTP client ────────────────────────────────────────────────────────────────
httpx>=0.27.2

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.3.3
pytest-asyncio>=0.24.0

# ── LangChain AI layer ─────────────────────────────────────────────────────────
langchain>=0.3.1
langchain-core>=0.3.6
langchain-groq>=0.2.1        # Groq provider (free tier — Llama 3.3 70B)
langchain-anthropic>=0.2.3   # Anthropic provider (Claude)
langchain-openai>=0.2.3      # OpenAI provider (GPT-4o)
langchain-community>=0.3.1
langgraph>=0.2.56            # Bonus 5: ReAct agent framework

# ── Security / Auth (Bonus 2) ──────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4       # bcrypt password/key hashing

# ── Rate limiting (Bonus 4) ────────────────────────────────────────────────────
slowapi>=0.1.9               # Rate limiting for FastAPI (Starlette-based)

# ── Caching (Bonus 4) ──────────────────────────────────────────────────────────
cachetools>=5.3.3            # In-process TTL cache for LLM results